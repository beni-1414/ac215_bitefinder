name: deploy-k8s
description: A Python program to deploy a containerized service to Google Cloud on Kubernetes clusters
runtime:
  name: python
  options:
    # Use the prebuilt project virtualenv instead of creating a new one
    virtualenv: /home/app/.venv
config:
  pulumi:tags:
    value:
      pulumi:template: python
