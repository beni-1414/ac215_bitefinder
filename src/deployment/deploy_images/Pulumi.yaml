name: deploy-images
description: A Python program to build & deploye images to GCP
runtime:
  name: python
  options:
    # Use the prebuilt project virtualenv instead of creating a new one
    virtualenv: /home/app/.venv
config:
  pulumi:tags:
    value:
      pulumi:template: python
